apiVersion: dapr.io/v1alpha1
kind: Component

metadata:
  name: statestore

spec:
  version: v1
  type: state.in-memory
  metadata: []
  # type: state.redis
  # metadata:
  # - name: redisHost
  #   value: localhost:6379
  # - name: redisPassword
  #   value: 